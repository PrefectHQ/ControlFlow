# Tasks

Tasks are a fundamental concept in ControlFlow, representing specific objectives or goals that AI agents work to accomplish. They serve as the primary unit of work in a ControlFlow application, encapsulating the what, how, and who of AI-driven operations.

## Creating tasks

The simplest way to create and execute a task is by using the `cf.run()` function: